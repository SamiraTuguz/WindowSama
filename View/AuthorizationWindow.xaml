<Window x:Class="WindowAuthorization.View.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowAuthorization.View"
        mc:Ignorable="d"
        Title="AuthorizationWindow" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Авторизация" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Blue"/>
        <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Center">
            <TextBlock Text="Логин" Foreground="Blue"/>
            <TextBox x:Name="tbLogin"/>
            <TextBlock Text="Пароль" Foreground="Blue"/>
            <TextBox x:Name="tbPassword"/>
        </StackPanel>
        <Button x:Name="btnSignIn" Content="Войти" Grid.Row="2"  Click="btnSignIn_Click"  Foreground="White"/>
    </Grid>
</Window>
